{
  "name": "zapformat-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for ZAPFORMAT",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "db:migrate": "psql \"$DATABASE_URL\" -f db/001_init.sql && psql \"$DATABASE_URL\" -f db/002_garage_owner_app.sql && psql \"$DATABASE_URL\" -f db/003_quote_requests.sql && psql \"$DATABASE_URL\" -f db/004_customer_workflow.sql && psql \"$DATABASE_URL\" -f db/005_vin_requests.sql && psql \"$DATABASE_URL\" -f db/006_checkout.sql && psql \"$DATABASE_URL\" -f db/007_operator_queue.sql"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
